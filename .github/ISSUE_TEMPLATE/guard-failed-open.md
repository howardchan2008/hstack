---
name: A guard let something through
about: The most useful report there is. It becomes a test case.
title: "[failed open] <hook>: <what got through>"
labels: failed-open
---

**Which hook**

<!-- e.g. risk-checkpoint.sh -->

**The payload it let through**

<!-- The exact command, or the tool_input. This is the part that becomes the
     test case, so verbatim beats paraphrase. -->

```
```

**What it cost**

<!-- Data lost, money spent, time. A number if you have one. Every hook in this
     repo was built from a sentence like this. -->

**What you expected it to do instead**

---

`./doctor.sh -v` output, if the hook might simply not have been armed:

```
```
